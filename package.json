{
  "name": "youtube-320p-downloader",
  "version": "1.0.0",
  "description": "A YouTube video downloader that downloads at 320p quality",
  "main": "index.js",
  "scripts": {
    "build": "pip install -r requirements.txt && mkdir -p netlify/functions"
  },
  "dependencies": {
    "axios": "^1.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
} 